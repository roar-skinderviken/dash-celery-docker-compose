# dash-celery-docker-compose
Plotly Dash-app using Celery/Redis for background processes.

### Runnning the app
```bash
docker-compose up --build
```

The app will be running at http://localhost:8080
